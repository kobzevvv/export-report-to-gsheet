# Local development example env vars (do not commit real secrets)

# Neon Postgres connection string. Keep sslmode=require.
NEON_DATABASE_URL=postgresql://username:password@host:5432/database_name?sslmode=require

# Optional shared secret checked by the Cloud Function. Change to a strong value.
PUBLIC_TRIGGER_TOKEN=change-me-strong-token

# GCP project and region for deploys
GCP_PROJECT_ID=your-gcp-project-id
GCP_CREDS_FILE_NAME=gbq_creds.json
GCP_REGION=us-central1

# Optional: function name and URL to display/use in Sheets
FUNCTION_NAME=pg_query_output_to_gsheet
GOOGLE_CLOUD_FUNCTION_URL=https://us-central1-your-project.cloudfunctions.net/pg_query_output_to_gsheet

# Local-only: if running the function locally with a service account key
# GOOGLE_APPLICATION_CREDENTIALS=gbq_creds.json
